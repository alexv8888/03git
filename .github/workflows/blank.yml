name: 'tab count'

on: push

jobs:

  tabcount:
    runs-on: ubuntu-latest
#    
        
    steps:
      - name: checkout
        uses: actions/checkout@v2.4.2
  
      - uses: lots0logs/gh-action-get-changed-files@2.1.4
